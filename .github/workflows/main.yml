name: CI
on:
  push:
    branches: [ ci-test ]
jobs:
  container-test-job:
    runs-on: ubuntu-latest

    steps:
      - run: sudo apt install kicad
      - run: kicad-cli version
      - run: pip install kikit
